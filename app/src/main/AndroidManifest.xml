<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.whw.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".draw.MyDrawViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".animation.AnimationSet2Activity"></activity>
        <activity android:name=".animation.XMLAnimatorActivity"></activity>
        <activity android:name=".animation.AnimatorSetActivity" />
        <activity android:name=".animation.ObjectAnimatorActivity" />
        <activity android:name=".animation.ValueAnimation2Activity" />
        <activity android:name=".animation.ValueAnimatorActivity" />
        <activity android:name=".animation.AnimationActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>